import type {Business} from "@/lib/data";
export default function BusinessCard({b}:{b:Business}){return <a href={`/negocio/${b.slug}`} className="group sld-card flex min-h-[235px] flex-col overflow-hidden rounded-2xl p-5">
  <div className="flex items-start justify-between gap-3"><div className="grid h-12 w-12 place-items-center rounded-2xl text-sm font-black text-black shadow-lg" style={{background:b.accent}}>{b.name.split(" ").map(w=>w[0]).slice(0,2).join("")}</div><div className="flex gap-1">{b.status==="verificado"&&<span className="rounded-full border border-emerald-400/20 bg-emerald-400/10 px-2 py-1 text-[10px] font-bold text-emerald-300">✓ VERIFICADO</span>}{b.demo&&<span className="rounded-full border border-amber-400/20 bg-amber-400/10 px-2 py-1 text-[10px] font-bold text-amber-300">DEMO</span>}</div></div>
  <div className="mt-5"><div className="flex items-center gap-2"><h3 className="font-bold tracking-tight group-hover:text-violet-200">{b.name}</h3>{b.open&&<span className="h-1.5 w-1.5 rounded-full bg-[var(--ok)]"/>}</div><p className="mt-1 text-xs capitalize text-[var(--muted)]">{b.category} · {b.address.split(",").pop()?.trim()}</p><p className="mt-3 line-clamp-2 text-sm leading-6 text-[var(--muted)]">{b.description}</p></div>
  <div className="mt-auto flex items-center justify-between border-t border-white/[.06] pt-4 text-xs"><span className="text-amber-300">★ {b.rating.toFixed(1)} <span className="text-[var(--muted2)]">({b.reviews})</span></span><span className={b.open?"text-emerald-300":"text-rose-300"}>{b.open?"Abierto ahora":"Cerrado"} · Ver →</span></div>
</a>}
