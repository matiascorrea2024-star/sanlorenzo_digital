// El WhatsApp de contacto de la plataforma ya no se hardcodea acá --
// es editable desde /perfil (solo admin) y se lee con
// lib/hooks/use-platform-settings.ts -> usePlatformSetting("whatsapp_contacto").
export const PLAN_PREMIUM_PRICE = 15000;
